/*
 * //NEEDS FINISHED
 * Goal: Create a function that takes in a number and finds the next doubleton number
 *
 * what is a doubleton number? A doubleton is a number that contains two distinct digits
 * example: 23,35,100,12121
 *
 *
 */

#include <iostream>

unsigned doubleton(unsigned int n){
//logic here
}





int main() {
    int number{0};
    //prompt for doubleton number
    std::cout << "Hello there, please place your doubleton number (two distinct digits): \n";
    std::cin >> number;
    //call function
    doubleton(number);
    return 0;
}
